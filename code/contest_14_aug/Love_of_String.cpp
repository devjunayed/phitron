#include<bits/stdc++.h>
using namespace std;

int main(){
    int k, n;
    cin >> n >> k;

    string s;
    cin >> s;

    vector<string> v;


    string s2;
    for(int i = 0; i < n-k; i++){
       string 
       v.push_back(sort(v2.begin()+i, v2.begin()+k));
    }

    return 0;
}